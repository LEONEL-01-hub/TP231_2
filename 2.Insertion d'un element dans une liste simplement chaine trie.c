#include <cstdlib>
#include <iostream>

using namespace std;
int main()
{
 int data;
 node*next;
 void insertsorted(node*&head,int val){
      node*p=newnode;
      p->data=val;
      p-.next=nullptr
      If(head== nullptr|| val <=head->data){
                p->next head;
                head=p;
                return;
                }
                node*cur= head
                while(cur->next !=nullptr && cur->next->data cur = cur->next
                }
                p->next= cur->next
                cur->next=p;
                void show(node*head){
                     node*tmp=head;
                     while(tmp!=nullptr){
                                         count<<tmp->data <<"->";
                                         tmp=tmp->next;
                                         }
                                         count <<"null" << endl;
                                         }
                                         int main() {
                                             node* head = nullptr;
                                             insertsorted(head,30);
                                             insertsorted(head,10);
                                             insertsorted(head,20);
                                             insertsorted(head,25);
                                             insertsorted(head,5);
                                             cout <<"sorted list:";
                                             show(head);
                                             while (head != nullptr){
                                                   node*t=head;
                                                   head=head->next;
                                                   delet t;                             
  system("RESUME");	
  return 0;
